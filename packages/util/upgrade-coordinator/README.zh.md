# @deepseek-ai/upgrade-coordinator

[English](README.md) | 中文

会话格式升级守护：读取持久化的格式版本，拒绝向下迁移，并让未触碰的存储保持原样。

## 模型体验

这里没有任何内容到达模型请求；此包守护跨启动的持久化会话格式版本。

#### KV Cache 影响

无；此包既不组装也不发送提供方请求。

## 已知限制与暂缓事项

- 本构建无法忠实执行的升级会被拒绝而不是猜测；就地格式迁移不在范围内。
